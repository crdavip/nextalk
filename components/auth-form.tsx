// Auth removed for MVP. Authentication was intentionally removed to
// simplify the initial product. If you need to re-enable authentication
// later, restore the previous `AuthForm` implementation.

export function AuthForm() {
  return null
}